# BO4DAC + RAG 系统 运行执行说明

> 本文是一份从零跑通的执行说明：环境 → 配置 → 建索引 → 启动 → 看效果。
> 系统是一个 Flask Web 应用：用贝叶斯优化为 DAC（直接空气捕集）CO₂ 捕集催化剂推荐配方，
> 并由 **本地 RAG 知识库（历史实验记录 + 可选 PDF 文献）+ Tavily 联网搜索** 共同为 LLM 建议提供带引用的依据。

---

## ⚠️ 0. 先决条件：代码库里**没有**的 4 样东西

`git` 仓库**只含代码**。以下 4 项被 `.gitignore` 忽略、需要自备：

`.env`  `data/historical_experiments.csv` `data/rag_chroma/`（向量库）
`Solid Sorbent Papers/`（MinerU 解析好的文献全文，可选但强烈建议——本地文献是主要证据源）

---

## 1. 环境要求

- **Python 3.10 或 3.11**（开发环境用的是 3.10）
- 约 **5 GB 磁盘**（依赖 + bge-m3 嵌入模型 ~2GB）
- 能联网（下载依赖、嵌入模型；运行时调用 DashScope/Tavily）
- 操作系统不限（macOS / Windows / Linux 均可）

## 2. 安装依赖

任选 conda 或 venv：

**方式 A：conda（推荐）**
```bash
conda create -n bo4dac python=3.10 -y
conda activate bo4dac
cd path/to/BO4DAC
pip install -r requirements.txt
```

**方式 B：venv**
```bash
cd path/to/BO4DAC
python -m venv .venv
# macOS / Linux:
source .venv/bin/activate
# Windows (PowerShell):
.venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

> `requirements.txt` 已补全（含 torch、botorch、chromadb、sentence-transformers、PyMuPDF 等），
> 体积较大，安装需几分钟。后文命令里的 `python` 都指**当前已激活环境里的 python**。

## 3. 放置 `.env` 和数据文件

1.  **`.env`** 放到 `BO4DAC/.env`。内容形如（密钥用对方给你的真实值替换）：
   ```ini
   DASHSCOPE_API_KEY=sk-你的DashScopeKey
   BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
   MODEL=qwen3.7-max
   ENABLE_THINKING=True
   ENABLE_WEB_SEARCH=True
   TAVILY_API_KEY=tvly-你的TavilyKey
   
   # ── RAG ──
   ENABLE_RAG=True
   RAG_DB_PATH=data/rag_chroma
   EMBEDDING_MODEL=BAAI/bge-m3
   RAG_TOP_K_PAPERS=6
   RAG_TOP_K_EXPERIMENTS=4
   RAG_MIN_SCORE=0.3
   RAG_MAX_CHUNKS_PER_PAPER=2      # 同一篇文献最多保留的检索 chunk 数
   WEB_CACHE_TTL_DAYS=7            # 联网搜索缓存有效期（天）
   # EMBEDDING_DEVICE=cpu          # 默认 cpu；Apple 芯片可设 mps；NVIDIA 显卡可设 cuda
   ```
2. 把 **`historical_experiments.csv`** 放到 `BO4DAC/data/historical_experiments.csv`
   （`data/` 目录不存在就新建一个）。
3. （可选但建议）把 MinerU 解析好的文献目录 **`Solid Sorbent Papers/`** 放到 `BO4DAC/` 根目录下，
   结构形如 `Solid Sorbent Papers/001.pdf-id/full.md`。

## 4. 构建 RAG 索引（首次必做）

```bash
python -m rag.ingest
```
- **首次运行会下载嵌入模型 bge-m3（约 2GB）**，请耐心等待。
- 成功结束应看到类似（5 篇解析文献 + 314 条实验记录）：
  ```
  INFO   001.pdf-id: 13 chunks (doi=10.1021/acs.iecr.9b00534) — A Simple Strategy To Improve …
  …
  INFO   built 314 experiment cards from 334 rows
  INFO   papers: stored 124/124
  INFO   experiments: stored 314/314
  INFO DONE. papers_chunks=124, experiments=314, db=data/rag_chroma
  ```
- 没放 `Solid Sorbent Papers/` 时 `papers_chunks=0` 也能正常运行（只用实验记录 + 联网）。
- 文献目录不在默认位置时用 `python -m rag.ingest --parsed-dir "路径"` 指定；
  另外裸 PDF 也可以丢进 `data/papers/` 一并入库（效果不如解析件，兜底用）。
- 以后**新增文献**：把新的 `NNN.pdf-id/` 文件夹放进 `Solid Sorbent Papers/`，重跑
  `python -m rag.ingest` 即可（幂等 upsert；若删除过旧文献建议 `--reset` 重建）。

> **模型下载慢（国内）怎么办**：在运行前设环境变量用镜像。
> macOS/Linux：`export HF_ENDPOINT=https://hf-mirror.com`；Windows PowerShell：`$env:HF_ENDPOINT="https://hf-mirror.com"`。
> 若你开了科学上网代理反而连不上镜像，把代理关掉再用镜像，或直连官方源。

## 5. 启动服务

```bash
python app.py
```
看到 `Running on http://127.0.0.1:5001` 后，浏览器打开 **http://127.0.0.1:5001**。

---

## 6. 怎么用 + 运行结果说明

### 6.1 网页操作流程
1. **Step 1 初始化搜索空间**：勾选载体（Support，如 SBA-15、MCM-41）、胺/添加剂（如 PEI、TEPA），
   设定有机含量范围，以及实验条件（温度、CO₂ 浓度、湿度、流速、测试方法）。
2. 进入生成步骤，点击 **「Get AI Suggestions」**（魔法棒图标按钮）。
3. 稍候——**首次点击会把 bge-m3 载入内存（约 10 秒）**，之后很快。

### 6.2 你会看到什么（这就是 RAG 的效果）
点击后页面会**实时**依次出现：

- **References（参考来源）面板**：列出 `[1] [2] …` 若干条，**混合三类来源**：
  - `实验记录: SBA-15 + TEPA → 2.17 mmol/g` 这类 → **本地 RAG**（你的历史实验数据）
  - `文献: A Simple Strategy To Improve PEI Dispersion …（§3. RESULTS AND DISCUSSION）` 这类
    → **本地 RAG**（`Solid Sorbent Papers/` 的文献全文，链接指向 DOI）
  - `Direct air CO2 capture using … (sciencedirect.com)` 这类 → Tavily 联网搜索
    （**带 7 天缓存**：同一 session 反复迭代不会重复调用；与本地文献重复的网页结果自动丢弃）
- **Thinking 面板**：模型的推理过程（流式滚动）。
- **AI 建议卡片**：最终 **5 条配方建议**，每条含载体/胺/有机含量/预测 CO₂ 容量，
  以及一段 **reasoning（带 `[N]` 引用标记）**——`[N]` 与上面 References 面板一一对应、可点击溯源。

**判断 RAG 是否生效的关键**：References 面板里**出现了 `实验记录: …` 和 `文献: …` 开头的条目**，
并且建议的 reasoning 里**引用了这些本地条目的编号**——说明模型确实在用你的历史实验数据和本地文献推理。
**判断缓存是否生效**：同一搜索空间下第二次点"Get AI Suggestions"，References 几乎瞬间出现
且联网条目与上次一致（后台日志显示 `web-search cache hit`）。

### 6.3 预期结果示例（真实跑出来的样子）
References 面板（节选）：
```
[1] 实验记录: SBA-15 + TEPA → 2.17 mmol/g        ← 本地 RAG
[2] 实验记录: SBA-15 + TEPA → 1.21 mmol/g        ← 本地 RAG
[3] 实验记录: SBA-15 + TEPA → 1.79 mmol/g        ← 本地 RAG
[4] 实验记录: SBA-15 + TEPA → 1.93 mmol/g        ← 本地 RAG
[5] Direct air CO2 capture using coal fly ash derived SBA-15 …   ← 联网
… （[6]–[12] 为更多联网文献）
```
某条建议的 reasoning（节选）：
> “SBA-15 supported PEI at 70 wt% achieved 1.92 mmol/g **[5]**. However, similar to TEPA on
> SBA-15 where capacity peaked at 60 wt% (2.17 mmol/g) and decreased at 70 wt% (1.93 mmol/g)
> due to pore blockage **[1, 4]**, a 50 wt% PEI loading … is proposed to balance …”

可见 `[1,4]` 是本地实验记录、`[5]` 是联网文献——两路证据融合、统一编号、可溯源。

---

## 7. 不开网页、命令行快速验证 RAG（可选，最快看效果）

把下面内容保存成 `BO4DAC/check_rag.py`，然后 `python check_rag.py`：

```python
from rag.rag_service import run_rag_retrieval, _build_rag_queries

sb = {"supports": ["SBA-15", "MCM-48"], "amine1": ["PEI", "TEPA"],
      "amine2": ["No"], "ocRange": [10, 70]}
cond = {"temperature": 25.0, "co2Concentration": 0.04, "humidity": 0,
        "flowRate": 100.0, "testMethod": "TGA"}

print("检索词:", _build_rag_queries(sb, cond))
items = run_rag_retrieval(sb, cond)          # 首次会加载 bge-m3，约 10 秒
print(f"\n命中 {len(items)} 条本地知识库记录：")
for it in items:
    print(f"  [{it['kind']}] 相似度={it['score']}  {it['title']}")
```
**预期输出**（数值因数据而异）：
```
检索词: ['PEI impregnated SBA-15 sorbent CO2 adsorption capacity direct air capture', …共4条]
命中 10 条本地知识库记录：
  [experiment] 相似度=0.687  实验记录: SBA-15 + TEPA → 2.17 mmol/g
  [experiment] 相似度=0.686  实验记录: SBA-15 + TEPA → 1.21 mmol/g
  [experiment] 相似度=0.684  实验记录: SBA-15 + TEPA → 1.79 mmol/g
  [experiment] 相似度=0.683  实验记录: SBA-15 + TEPA → 1.93 mmol/g
  [paper] 相似度=0.705  文献: Adsorption of CO2 in Flue Gas by Polyethylenimine-Functionalized …
  [paper] 相似度=0.702  文献: A comprehensive evaluation of amine-impregnated silica materials …
  …（paper 最多 6 条，同一篇文献最多 2 条）
```
能打印出 `实验记录` 和 `文献` 两类条目即表示 RAG 检索链路完全正常。

---

## 8. 常见问题排查

| 现象 | 原因 / 解决 |
|---|---|
| `ModuleNotFoundError` | 没激活虚拟环境，或没 `pip install -r requirements.txt` |
| ingest 报连接 huggingface 失败 | 网络问题。设 `HF_ENDPOINT=https://hf-mirror.com` 用镜像（见 §4），或开/关代理重试 |
| References 里**没有** `实验记录` 条目 | RAG 库为空：确认已放 `historical_experiments.csv` 并成功跑过 `python -m rag.ingest`；`.env` 里 `ENABLE_RAG=True` |
| References 里**没有** `文献:` 条目 | 没放 `Solid Sorbent Papers/` 或放了没重跑 `python -m rag.ingest`（跑完应显示 `papers: stored N/N`） |
| 联网结果一直不更新 | 缓存生效中（默认 7 天）。想强制刷新：删掉 `data/web_search_cache.json`，或把 `.env` 里 `WEB_CACHE_TTL_DAYS` 调小 |
| 点建议报 `DASHSCOPE_API_KEY not set` | `.env` 缺失或没放对位置（必须在 `BO4DAC/.env`），或 Key 无效 |
| 联网来源为空但本地有 | Tavily Key 无效/额度用尽/无网，不影响本地 RAG；可在 `.env` 设 `ENABLE_WEB_SEARCH=False` 只看本地 |
| 端口 5001 被占用 | 关掉占用进程，或改 `app.py` 末尾的 `port=5001` |
| 首次点建议卡住约 10 秒 | 正常，bge-m3 正在载入内存，仅首次 |
| 想纯离线、只用本地库 | `.env` 设 `ENABLE_WEB_SEARCH=False`（保留 `ENABLE_RAG=True`） |

---

## 9. 一页速查（TL;DR）

```bash
# 0) 先拿到 .env、data/historical_experiments.csv、Solid Sorbent Papers/（代码库里都没有）
# 1) 装环境
conda create -n bo4dac python=3.10 -y && conda activate bo4dac
pip install -r requirements.txt
# 2) 放好 .env(根目录)、data/historical_experiments.csv、Solid Sorbent Papers/(根目录)
# 3) 建索引（首次下载 bge-m3 ~2GB）
python -m rag.ingest       # 应看到 papers: stored 124/124, experiments: stored 314/314
# 4) 启动
python app.py        # 打开 http://127.0.0.1:5001 → Step1 初始化 → Get AI Suggestions
```
看到 References 面板里有 `实验记录: …` 和 `文献: …` 条目、建议 reasoning 引用了它们，
且第二次生成建议时联网来源秒出（缓存）= 本次全部功能跑通 ✅
